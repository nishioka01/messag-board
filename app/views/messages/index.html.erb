<div class="panel panel-default">
    <div class="panel-heading">
        メッセージ一覧
        </div>
        
<div class="panel-body">
<% @messages.each do |message| %>


 <span class="label label-info">
     <%= message.name %>
     </span>
           
            
<div class="form-control" rows="12">
        <%= message.body %>
        </div>

        <br>
        <% end %>
        </div>
        
<%= form_for(@message) do |f| %>
名前：
<%= f.text_area :name %>

内容：

<%= text_area :page,  :body  %>

<% end %>
